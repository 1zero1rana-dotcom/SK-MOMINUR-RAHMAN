rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow all reads and writes for debugging the 0% stuck issue
    // WARNING: This is insecure and should be hardened before production
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
